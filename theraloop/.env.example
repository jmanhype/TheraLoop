# Provider keys (optional; mock used if absent)
TOGETHER_API_KEY=
# Experiment naming
THERALOOP_EXPERIMENT=theraloop-demo
# Router threshold for logprob sum
THERALOOP_CONFIDENCE_THRESHOLD=-50.0
# Calibration file (generated by calibration script)
THERALOOP_CALIBRATION_FILE=outputs/calibration.json
